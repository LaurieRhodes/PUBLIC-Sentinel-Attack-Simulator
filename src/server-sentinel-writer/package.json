{
  "name": "@modelcontextprotocol/server-sentinel-writer",
  "version": "0.0.1",
  "description": "MCP server for Sentinel data ingest via Data Collection Rules (DCR's)",
  "license": "MIT",
  "author": "Laurie Rhodes (https://laurierhodes.info)",
  "homepage": "https://laurierhodes.info)",
  "bugs": "",
  "type": "module",
  "bin": {
    "server-sentinel-writer": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "0.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
